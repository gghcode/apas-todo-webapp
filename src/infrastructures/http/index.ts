// import { getMethod, postMethod, putMethod, deleteMethod } from './effectFuncs';
import { Agent } from './agent';
import { Request } from './request';
import { RequestBuilder } from './requestBuilder';
import { setBody } from './effectFuncs';
import { bindToken, setToken } from './token';

export {
  Agent,
  Request,
  RequestBuilder,
  setBody,
  setToken,
  bindToken,
  // getMethod,
  // postMethod,
  // putMethod,
  // deleteMethod,
};
